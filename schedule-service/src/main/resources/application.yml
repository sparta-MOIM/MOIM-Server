server:
  port: 8086

spring:
  application:
    name: schedule-service
  config:
    import:
      - "application-database.yml"
      - "application-external.yml"
  security:
    user:
      name: user
      password: 1234
